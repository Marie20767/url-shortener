version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries.sql"
    schema: "db/migrations" # sqlc scans all .sql files to build schema
    gen:
      go:
        package: "db"
        out: "internal/db"
