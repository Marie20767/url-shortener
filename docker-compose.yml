services:
  postgres:
    image: postgres
    container_name: url_keys_container
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: url_keys
    volumes:
      - ./internal/store/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5433:5432"