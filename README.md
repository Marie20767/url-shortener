## Implementation of a lightweight url shortener in Go using:

- PostgreSQL, MongoDB and Redis for storage.

## Running the app

- Start the app:
```make start```

- Stop the database:
```make stop-db```

- Start the database:
```make start-db```
